apiVersion: 1

datasources:
  - name: claudedog
    uid: claudedog
    type: frser-sqlite-datasource
    access: proxy
    isDefault: false
    jsonData:
      path: /var/lib/grafana/claudedog.db
    editable: true
