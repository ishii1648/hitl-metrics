apiVersion: v1
kind: Service
metadata:
  name: agent-telemetry-server
spec:
  type: NodePort
  ports:
    - name: http
      port: 8443
      targetPort: http
      nodePort: 30843
      protocol: TCP
